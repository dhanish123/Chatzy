CHATZY - QUICK START GUIDE

GET RUNNING IN 5 MINUTES:

STEP 1: CLONE & SETUP (2 min)
cd c:\Users\dhani\Desktop\2027\RealChat

Backend:
  cd server
  npm install
  cp .env.example .env
  # Update .env with MongoDB URL and JWT_SECRET

Web:
  cd ../web
  npm install
  cp .env.example .env
  # Update .env with backend URL

Mobile (optional):
  cd ../mobile
  npm install
  cp .env.example .env

STEP 2: RUN LOCAL (3 min)

Terminal 1 - Backend:
  cd server
  npm start
  # Should output: "Server running on port 5000"

Terminal 2 - Web:
  cd web
  npm run dev
  # Should output: "Local: http://localhost:5173"

Terminal 3 - Mobile (optional):
  cd mobile
  expo start
  # Scan QR code with Expo Go app

STEP 3: TEST

Web: Open http://localhost:5173
- Register new account
- Create another account in incognito window
- Test messaging between accounts
- Test real-time features

Mobile:
- Install Expo Go app
- Scan QR code
- Same tests as web

ENVIRONMENT VARIABLES QUICK REFERENCE:

Backend (.env):
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/chatzy
JWT_SECRET=your-secret-key-min-32-chars
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
API_URL=http://localhost:5000
SOCKET_URL=http://localhost:5000

Web (.env):
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000

Mobile (.env):
EXPO_PUBLIC_API_URL=http://localhost:5000/api
EXPO_PUBLIC_SOCKET_URL=http://localhost:5000

DEPLOYMENT QUICK STEPS:

1. Backend to Railway:
   - Push to GitHub
   - Connect to Railway
   - Set env vars
   - Deploy

2. Web to Vercel:
   - Import from GitHub
   - Set env vars
   - Deploy

3. Mobile to Expo:
   - eas build --platform android
   - Follow prompts

COMMON ISSUES:

Port 5000 already in use:
  - Kill process: lsof -ti:5000 | xargs kill -9
  - Or change PORT in .env

MongoDB connection failed:
  - Check connection string
  - Verify IP whitelisted in Atlas
  - Check network connectivity

Socket.IO not connecting:
  - Backend must be running
  - Check SOCKET_URL in .env
  - Check browser console for errors

API returns 401:
  - Token expired, logout and login again
  - Check JWT_SECRET matches

File uploads not working:
  - Check /uploads directory exists
  - Check write permissions
  - Check file size limits

FEATURES TO TRY:

1. Register & Login
   - Create account
   - Login

2. Friends
   - Search for user
   - Send friend request
   - Accept request in other window

3. Messaging
   - Send text message
   - See real-time delivery
   - Edit message (within 10 min)
   - Delete message (within 10 min)
   - Reply to message

4. Online Status
   - See online status
   - See last seen
   - See typing indicator

5. Groups
   - Create group with 2+ members
   - Send group message
   - Add member
   - Leave group

6. Blocking
   - Block user
   - Try sending message (fails)
   - Unblock user

7. File Upload
   - Upload image/video/audio
   - See in message
   - Download file

USEFUL COMMANDS:

Backend:
  npm start          # Start server
  npm run dev        # Dev with nodemon

Web:
  npm run dev        # Start dev server
  npm run build      # Build for production
  npm run preview    # Preview build

Mobile:
  expo start         # Start dev
  expo start --ios   # iOS
  expo start --android # Android

NEXT STEPS:

1. Test locally
2. Fix any issues
3. Deploy backend
4. Deploy web
5. Deploy mobile
6. Configure domain
7. Monitor deployment

See DEPLOYMENT.txt for detailed steps
See TESTING.txt for comprehensive tests
See SETUP.txt for more configuration options

SUPPORT:

Stuck? Check:
1. Terminal output for errors
2. Browser console (F12)
3. Backend logs
4. MongoDB connection
5. Network tab in DevTools
6. TESTING.txt for common issues
